# python-practice-tasks
A collection of beginner-friendly Python practice tasks covering strings, lists, dictionaries, loops, and problem-solving concepts. Perfect for learning and revising core Python skills.
# Python Practice Tasks

![Python 3.10+](https://img.shields.io/badge/python-3.10%2B-blue) ![License: MIT](https://img.shields.io/badge/license-MIT-green)

A collection of beginner-friendly Python exercises covering strings, lists, dictionaries, loops, and problem-solving concepts. Each task includes a clear explanation and an interactive example you can run locally.

---

## 🚀 What’s inside
This repository contains a single, easy-to-run file:

- `python_practice_tasks.py` — Interactive menu-driven script implementing 10 fundamental tasks:
  1. Count vowels in a string  
  2. Extract unique elements from two lists (elements in either but not both)  
  3. Word frequency counter  
  4. Swap keys and values in a dictionary (safe grouping if values duplicate)  
  5. Find maximum and minimum without using `max()` or `min()`  
  6. Remove duplicates from a list while preserving order  
  7. Tuple unpacking (skip middle values)  
  8. Merge two dictionaries (add values for duplicate keys)  
  9. Check if a string is a palindrome  
  10. Find common characters between two strings

---

## ✅ Badges & License
- Python version: 3.10+ recommended.  
- License: MIT (feel free to change).  
You can keep the badges at the top to show compatibility and license.

---

## ✨ Features
- Interactive CLI menu — choose which task to run.
- Clean, commented code (easy to read & modify).
- Safe handling for edge cases (empty inputs, duplicates, punctuation).
- Good for learning, interview practice, and sharing with peers.

---

## 💻 How to run

1. Clone the repo:
```bash
git clone https://github.com/your-username/python-practice-tasks.git
cd python-practice-tasks
